local types = require("../../types")

local buttonSelected: types.PartialConfig = {
	ButtonTransparency = 0.75,
	ButtonHoveredTransparency = 0.75,
	ButtonActiveTransparency = 0.5,
}

return buttonSelected
